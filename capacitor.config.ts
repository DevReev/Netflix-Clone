import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.veermaruthesh.netflix.com',
  appName: 'netflix',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
